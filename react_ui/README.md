# React UI

Minimal React frontend for AI Tutor.

## Environment

Copy `.env.example` to `.env`:

```bash
cp .env.example .env
```

Required values:

- `VITE_API_BASE_URL`
- `VITE_WS_URL`

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
